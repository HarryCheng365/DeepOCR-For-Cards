server:
  port:
    8080
spring:
  http:
    encoding:
      charset:
        UTF-8
      enabled:
        true
      force:
        true
  messages:
    encoding:
      UTF-8
  jackson:
    default-property-inclusion:
      non_null
  datasource:
    driver-class-name:
      com.mysql.jdbc.Driver
    url:
      jdbc:mysql://localhost:3306/db_person?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username:
      root
    password:
      Chy908798

  jpa:
    database-platform:
      org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate.ddl-auto:
      update
    show-sql:
      true